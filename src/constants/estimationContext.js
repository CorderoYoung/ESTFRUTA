/** Valores literales ComboBox (sin tablas catálogo). */
export const COSECHA_OPTIONS = Object.freeze(['PC', 'RC']);

export const TIPO_ESTIMACION_OPTIONS = Object.freeze(['C1', 'C2', 'C5']);

/** Clave sessionStorage para reanudar cabecera tras F5 en paso 2. */
export const ESTIMACION_HEADER_STORAGE_KEY = 'estfruta_estimacion_header';
